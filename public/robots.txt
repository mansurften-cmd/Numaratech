# Pre-launch build on a temporary host.
#
# numaratech.com is not yet purchased. Until it is, this origin must not
# accumulate index history that would later have to be unpicked — so indexing
# is blocked entirely rather than merely canonicalised away.
#
# On launch: remove the Disallow below, remove X-Robots-Tag from public/_headers,
# and keep this file's Disallow permanently on *.workers.dev.
User-agent: *
Disallow: /

Sitemap: https://numaratech.mansurften.workers.dev/sitemap.xml
